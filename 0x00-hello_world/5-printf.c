#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main()
{
	char str1[] = "With proper grammar, but the outcome is a piece of art,\n";

	printf("%s",str1);
	return 0;
}
