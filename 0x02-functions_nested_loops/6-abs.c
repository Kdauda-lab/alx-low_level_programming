#include "main.h"
/**
 * _abs - return the absolute value of an integer
 * @n: take an integer arguement
 * Return: an integer
 */
int _abs(int n)
{
	if (n < 0)
	{
		int r = n * -1
		_putchar(r + '0');
	}
	else
	{
		_putchar(n + '0'i);
	}		
return (0)
}
